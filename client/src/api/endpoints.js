export const ENDPOINTS = {
    // Ping
    ping: "/ping",
    // Auth
    register: "/register",
    // Contact
    contact: "/contact",

    // users
    users: "/users",
    // Add more endpoints as needed here
};
